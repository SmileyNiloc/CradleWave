services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always

  processor:
    build:
      context: ../webdev/backend/processor
      dockerfile: Dockerfile
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    restart: always
